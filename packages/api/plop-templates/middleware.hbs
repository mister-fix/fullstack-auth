import { NextFunction, Request, Response } from 'express';

/**
	* Middleware to (add middleware description here).
	*/
export const {{camelCase name}} = (
	req: Request,
	res: Response,
	next: NextFunction
) => {

};
